# 웹접근성과 웹표준 

## 웹표준

- 동일한 환경에서 동일한 모습을 보여주는 것 

## 웹접근성

> 장애 여부와 상관없이 누구나 원활하게 웹페이지를 이용할 수 있어야 한다는 것

- 모든 이미지에는 대체 텍스트를 제공
- 소리없이 화면으로 진행되는 경우 별도의 텍스트나 음성으로 해당정보를 제공
- 글자와 배경간의 명도대비 와 폰트크기를 충분히 제공
- 키보드만으로도 모든 정보 확인 및 기능을 이용할수 있드록 구현 

# HTML5

## 콘텐츠 모델 

- 요소별로 비슷한 성격을 가지고 있는 것끼리 그룹화 시킨 것

### 카테고리

- 메타데이터 컨텐츠
- 플로우 컨텐츠
- 구획 컨텐츠
- 제목 컨텐츠
- 구문 컨텐츠
- 내장형 컨텐츠
- 대화형 컨텐츠
- 기타등등...

## 아웃라인 알고리즘 

- 정보 구조를 명확하게 하는 것

    ```html
    <body>
    <header>
        <h1>HTLM5</h1>
        <nav>
            ...
        </nav>
    </header>
    <section>
        <h1>콘텐츠모델</h1>
        <p>...</p>
    </section>
    <section>
        <h1>...</h1>
        <article>
            <h1>...</h1>
        </article>
    </section>
    <footer>
        <h1>...</h1>
    </footer>
    ```

    > 같은 레벨로 작성 하는 게 아니라 계층이 분류되는 것을 말한다.

## HTML5 API

1. Web Storage
1. Web SQL Database/Indexed Database
1. Application Cache
1. Web Workers
1. Web Socket
1. Notifications
1. File API
1. Geolocation
1. Device Orientation
1. Speech Input
1. Drag & Drop

## markup의 기초

- HTML 문서는 요소와 태그 그리고 콘텐츠로 구성 , 요소는 HTML의 의미를 가진 개념이다.

## DTD

## HTML 과 XHTML 의 차이

## 단축키

- `^↑k` : 한줄 삭제

## HTML 구성


```html
<html lang="ko-KR">
```

> `lang`속성 : 언어 지정 및 값을 통해 스크린 리더가 인식

```html
<meta charset="UTF-8">
```

> `charset="utf-8"` : 다국어 인코딩 지원 한다 `utf-8`로 사용해야 한다.

> `charset="utf-8"` 을 `<title>웹카페-HTML5,CSS3,웹접근성</title>` 밑에 두어선 안된다 글자가 깨질 수가 있다.

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

> `viewport`는 개발자 들이 뷰포트의 크기와 스케일 조정하게 만든것 

> `width`는 뷰포트의 크기를 조정한다 `device-width`는 100% 스케일에서 CSS 픽셀들로 계산된 화면의 폭을 의미한다.

```html
<meta http-equiv="X-UA-Compatible" content="ie=edge">
```

> `http-equiv`는 문서의 초기정보를 나타내는 속성이다 `X-UA-Compatible`는 브라우저호환성 설정이다. 

> `content="ie=edge"`는 최신버전으로 문서모드를 설정하라는 것(현재 설치된 최신버전을 쓴다) 

```html
<title>웹카페-HTML5,CSS3,웹접근성</title>
```

> 타이틀은 그 페이지의 중심문장을 쓴다 왜냐하면 SEO가 페이지 단위로 접근하기 때문이다.

## HTML SNIPPETS 설정

- `.vscode\extensions\abusaidm.html-snippets-0.2.1\snippets` 에 가서 `snippets.json` 파일을 연다음 주석을 풀고 프로그램을 다시 킨다.


`<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">`

> `-`로 시작하면 기업표준이라는 것 

- - - 

```html
<link rel="shortcut icon" href="" type="image/x-icon">
```

css 외부 파일을 연결하는 것을 익스터널 스타일 방식이다.

# 컨텐츠 중심으로 마크업해야한다

> 성격이 같은 컨텐츠 끼리 나누거나 묶는 작업이 필요하다

- - - 

- 헤더
- 메인 
- 푸터

3단 기본적인 골격

4단 골격

- 헤더
- 네비게이션
- 메인
- 푸터

 - - - 

 헤더는 브랜딩 영역을 포함시킨다. 

 브랜딩과 네비게이션을 포함시킨것이다.

블럭으로 

푸터의 핵심은 저작권정보, 주소 정보이다 

- - - 

- 헤더 
- 비쥬얼
- 메인
- 슬로건
- 푸터 

이 전체 5그룹을 묶을것이다.

1. 이제 시멘틱 마크업을 할 것이다 의미를 부여하는 단계

- div (중립적인 컨텍츠 일 경우)
- header 
- main
- article (섹션은 장 단위로 나눈것 / 아티클은 독립적인 정보에 쓰인다.)
- footer 

- 묶어 놓는것은 div 태그이다.

1. 마크업 절차가 끝나면 네이밍 하는 방법이 있다.

    1. 아이디 방식 (유니크)(#)
    1. 클래스 방식 (여러번 재사용 가능)(.)

    클래스이름은 이름만 보고 내용을 알수 있게 만든다.

    > 네이밍 규칙 pc cc sc cc 

    > pc MainMenu 파스칼케이스
    > cc mainMenu 카멜 케이스
    > sc main_menu 스네이크 케이스
    > cc main-menu 케바케이스 

    > BEM 검색

    - 헤더태그에 클래스 이름을 header 라고 준다.
    - 비쥬얼 영역은 .visual
    - 메인은 .main
    - 아티클은 .slogan
    - 푸터는 footer

    - 바깥 div는 wrap wrapper , container 를 쓴다
    - .container 라고 준다.

    > 바깥을 감싸는 것은 넓이를 조정할수 있다. 

    + : 는 형제 태그
    > : 자식 태그 

    워닝 : 은 무시 해도 된다

    article 은 명시적인 아웃라인을 가진다 그래서 헤딩을 포함해야한다

    섹션 나브 아티글 어싸인 태그 이다 섹셔닝 태그이다.


    단축키 등록 하는 방법

    바로 가기 키 emmet에서 찾아서 키를 등록한다.

    body 자체에는 width가 안 먹힌다. 

1. 배치 하기 위한 컬럼 나누기

main 안에 3개 div를 만든다
네이밍은 .group 이다
클래스는 중복 할 수가 있다.
문제는 3개의 너비가 다르다.
구분하는 방법은 몇번째 자식 인지로 할수 있다.
하지만 힘들어서 이름을 또 줄 것이다.

group1 , group2, group3

* : 원하는 숫자 만큼 $ : 해당하는 넘버로 대체가 된다.
${글자} : <div class="group group1">그룹1</div>

줄바꿈이 되는것은 블록이라서 

user-agent : 는 브라우저를 말한다.

그래서 크로스 브라우징이 생긴다.

그래서 에이전트 스타일을 재정의 하면 된다.

`m0` 하면  margin: 0;

css는 스타일이 겹친다.

우선순위가 높다 유저에이전트보다 작성한 코드가 우선순위가 더 높다.

em 상속받은 글자크기 를 곱한 것이다.
바디의 기본 글자 크기는 16px이다.

접두사를 벤더 프리픽스라고 한다.

-webkit- 크롬 사파리 오페라
-ms-
-moz-
그 브라우저에서만 작동한다.


`,`는 그룹핑 연산자이다.

기존의 스타일을 초기화 시키는 것을 리셋 스타일이라고 한다.

> 배치관점이 아닌 컨텐츠 관점으로 봐야한다.

> 높이는 고정하지 않는게 추천한다.

헤더 높이는 110px
비주얼 120px
main auto
slogan auto
footer auto

g1 250px
g2 380px;
g3 190px;
g1+g2+g3 : margin 30px;

위 아래 여백 30

> 설계할때 크기를 부터 계산해야한다

베이직 박스 모델이다.

slogan , footer 위 아래 여백 10 

`^/` 하면 주석 단축키

rgb 6자리가 반복되면 단축 할수 있다

vh : 는 화면에 보이는 영역까지 출력된다. 
바디는 100%를 할수 없다. 

모든 요소의 백그라운드가 투명이다. 

색갈로 영역을 구분하기 위햇 ㅓ설정한다.

마진 auto 하면 마진을 계산해서 반을 나누어서 넣는것이다.

마진 auto는 정렬한것처럼 보이게 할수 있다.

margin : 시계방향이다. 탑 오 아 왼

단축키 `알트 방향키 ` 위 아래로 이동

플래스 명령의 요소박스의 부모에 넣어야한다
부모의 높이 만큼 상속받는다. 
스트레칭 되는건 패딩을 뺴고 나머지만큼 늘어난다.

메인축 교차축이 있다. 기본값이 
flex-direction: row; 이 기본값이 x축방향으올 정렬이 된다.
x축 메인축 
컬럼이면 y축이 메인축

justify-content: flex-end; 하면 오른쪽 정렬이 된다.
center  가로가 가운데 정렬이 된다.

space-between; 사이사이간격을 띄워준다.
space-around; 

space-evenly; 균등하게 여분이 된다

플렉스 박스의 장점 
여백이 유연하게 줄었다 늘었다가 한다.

알아서 비율 대로 줄인다. 부모의 너비를 넓혀도 
